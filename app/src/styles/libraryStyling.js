import {StyleSheet} from 'react-native';

const libraryStyling = StyleSheet.create({
  heading: {
    color: '#fff',
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 16,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  songContainer: {
    padding: 10,
    backgroundColor: '#343a40',
    width: '100%',
    borderColor: '#2AC062',
    borderWidth: 1,
    borderRadius: 5,
    marginBottom: 10,
  },
  songTitle: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },
});

export default libraryStyling;
